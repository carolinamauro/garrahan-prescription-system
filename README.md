## README

- `npm install` (para instalar las dependencias)
- `npm run build` (primera vez)
- `npm start` (iniciar el servidor)
- `npm run dev` (desarrollo)
- `npx eslint . --ext .js,.jsx --fix` (solucionar automaticamente problemas de linter)

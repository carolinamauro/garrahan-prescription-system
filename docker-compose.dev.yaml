services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: dev_env_frontend
    ports:
      - "3003:3003"
      - "3004:3004"
    volumes:
      - ./:/app
    working_dir: /app
    tty: true
    stdin_open: true
